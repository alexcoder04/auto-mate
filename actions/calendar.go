package actions

func CalendarAdd(i map[string]any) map[string]any {
	// $HOME/.local/share/evolution/calendar/system/calendar.ics
	// parse with some ics library
	return map[string]any{
		"success": false,
	}
}
